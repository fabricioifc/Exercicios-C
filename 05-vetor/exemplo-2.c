# include <stdio.h>

int main(void) {
    int vetor[5], i=0;

    scanf("%d", &vetor[i++]);
    scanf("%d", &vetor[i++]);
    scanf("%d", &vetor[i++]);
    scanf("%d", &vetor[i++]);
    scanf("%d", &vetor[i++]);

    printf("%d\n", vetor[0]);

    return 0;
}