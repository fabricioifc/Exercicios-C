# include <stdio.h>

int main(void) {
    int vetor[5];

    scanf("%d", &vetor[0]);
    scanf("%d", &vetor[1]);
    scanf("%d", &vetor[2]);
    scanf("%d", &vetor[3]);
    scanf("%d", &vetor[4]);

    printf("%d\n", vetor[0]);

    return 0;
}